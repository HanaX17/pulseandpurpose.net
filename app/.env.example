# Copy to `.env` and fill from `supabase start` output (local) or your project
# settings (cloud). These are passed at build time via --dart-define-from-file.
#
#   flutter run --dart-define-from-file=.env
#
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=your-anon-key-here
